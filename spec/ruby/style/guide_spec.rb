RSpec.describe Ruby::Style::Guide do
  it "has a version number" do
    expect(Ruby::Style::Guide::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
